# extract_PDF

Инструмент для извлечения текста из PDF-документов с автоматическим разделением больших документов на части для удобной работы с LLM (Large Language Models).

## Описание

Скрипт автоматически обрабатывает все PDF-файлы в указанной директории, извлекает из них текст и сохраняет результаты. Для больших документов скрипт автоматически разделяет текст на 2-3 части, что позволяет загружать их в LLM по частям.

## Основные возможности

- ✅ Автоматическое извлечение текста из всех PDF-файлов в директории
- ✅ Рекурсивный поиск PDF в подпапках
- ✅ Сохранение с нумерацией страниц
- ✅ Автоматическое разделение больших документов на части
- ✅ Умное разделение по абзацам (не разрывает предложения)
- ✅ Поддержка UTF-8 кодировки
- ✅ Настраиваемые параметры через аргументы командной строки
- ✅ Детальная статистика обработки
- ✅ Улучшенная обработка ошибок
- ✅ Автоматическая очистка имен файлов от недопустимых символов

## Требования

- Python 3.6+
- Библиотека `pdfplumber`

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/JohnKramer-CMD/extract_PDF.git
cd extract_PDF
```

2. Установите необходимые зависимости:
```bash
pip install pdfplumber
```

## Использование

### Базовое использование

1. Поместите PDF-файлы в директорию со скриптом
2. Запустите скрипт:
```bash
python extract_and_save.py
```

3. Результаты будут сохранены в папке `extracted_texts/`

### Аргументы командной строки

Скрипт поддерживает различные аргументы для гибкой настройки:

```bash
python extract_and_save.py [ОПЦИИ]
```

#### Основные опции:

- `-d, --directory DIR` - Директория с PDF файлами (по умолчанию: текущая)
- `-o, --output DIR` - Директория для сохранения результатов (по умолчанию: `extracted_texts`)
- `-r, --recursive` - Рекурсивный поиск PDF файлов в подпапках
- `--max-chars N` - Максимальное количество символов на часть (по умолчанию: 50000)
- `--min-parts N` - Минимальное количество частей (по умолчанию: 2)
- `--max-parts N` - Максимальное количество частей (по умолчанию: 3)
- `-h, --help` - Показать справку

#### Примеры использования:

```bash
# Обработка PDF из другой папки
python extract_and_save.py -d ./my_pdfs

# Сохранение результатов в другую папку
python extract_and_save.py -o ./results

# Рекурсивный поиск PDF в подпапках
python extract_and_save.py -r

# Комбинирование опций
python extract_and_save.py -d ./documents -o ./output -r

# Настройка параметров разделения
python extract_and_save.py --max-chars 100000 --max-parts 5

# Полная настройка
python extract_and_save.py -d ./pdfs -o ./texts -r --max-chars 75000 --min-parts 2 --max-parts 4
```

## Формат выходных файлов

### Маленькие документы (≤ 50,000 символов по умолчанию)
- Сохраняются как один файл: `имя_документа.txt`

### Большие документы (> 50,000 символов по умолчанию)
- Автоматически разделяются на 2-3 части (настраивается):
  - `имя_документа_часть1_из3.txt`
  - `имя_документа_часть2_из3.txt`
  - `имя_документа_часть3_из3.txt`

Каждая часть содержит полный текст с нумерацией страниц, что позволяет загружать их в LLM по отдельности.

**Примечание:** Имена файлов автоматически очищаются от недопустимых символов (например, `<>:"/\|?*`).

## Настройки

### Через аргументы командной строки (рекомендуется)

```bash
python extract_and_save.py --max-chars 50000 --min-parts 2 --max-parts 3
```

### Через редактирование кода

В начале файла `extract_and_save.py` можно настроить параметры по умолчанию:

```python
MAX_CHARS_PER_PART = 50000  # Максимальное количество символов на часть
MIN_PARTS = 2                # Минимальное количество частей
MAX_PARTS = 3                # Максимальное количество частей
```

### Рекомендации по настройке для LLM:

- **GPT-3.5/GPT-4**: `--max-chars 50000` (примерно 12,000-15,000 токенов)
- **Claude**: `--max-chars 60000` (примерно 15,000-18,000 токенов)
- **Другие модели**: настройте в зависимости от лимита токенов вашей модели

## Пример работы

```
🔍 Найдено PDF файлов: 3
📁 Результаты будут сохранены в: D:\root\Coursor\extract_PDF\extracted_texts

📄 Обработка: small_document.pdf
  ✅ Сохранено: small_document.txt
     📊 Размер: 15,234 символов, 5 страниц

📄 Обработка: large_document.pdf
  📦 Документ большой (125,000 символов), разделяем на 3 части...
  ✅ Сохранено: large_document_часть1_из3.txt (41,667 символов)
  ✅ Сохранено: large_document_часть2_из3.txt (41,667 символов)
  ✅ Сохранено: large_document_часть3_из3.txt (41,666 символов)
     📊 Всего частей: 3, 45 страниц

📄 Обработка: empty_document.pdf
  ⚠️  Файл пустой или не содержит текста

============================================================
📊 ИТОГОВАЯ СТАТИСТИКА
============================================================
✅ Успешно обработано: 2
📦 Разделено на части: 1
⚠️  Пустых файлов: 1
❌ Ошибок: 0
📝 Всего символов извлечено: 140,234
============================================================
```

## Особенности разделения

Скрипт использует умный алгоритм разделения:
- Разделение происходит по абзацам (двойной перенос строки)
- Не разрывает предложения и абзацы
- Равномерно распределяет текст между частями
- Сохраняет нумерацию страниц в каждой части
- Автоматически обрабатывает крайние случаи (мало абзацев, пустой текст)

## Обработка ошибок

Скрипт включает улучшенную обработку ошибок:
- Детальные сообщения об ошибках для каждого файла
- Продолжение работы при ошибках в отдельных файлах
- Предупреждения о пустых PDF или файлах без текста
- Валидация входных данных и директорий

## Статистика

После обработки скрипт выводит подробную статистику:
- Количество успешно обработанных файлов
- Количество файлов, разделенных на части
- Количество пустых файлов
- Количество ошибок
- Общее количество извлеченных символов

## Лицензия

MIT License

## Автор

JohnKramer-CMD
